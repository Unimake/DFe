# Readiness

`ABI-001` está em `DELIVERED_FOR_REVIEW` na `attempt-0003` e aguarda revisão do DEV. O plano ativo reside integralmente em `docsplan/nfabi`, está restrito à DLL Unimake.DFe e aos testes deste repositório; produtos consumidores externos possuem planejamento separado. O congelamento dos IDs candidatos e o estado `READY_FOR_EXECUTION` só se tornam efetivos com `ABI-001 APPROVED`.

`ABI-002` permanece `PLANNED` e não foi iniciada. As etapas `ABI-002+` exigem `ABI-001 APPROVED`, autorização explícita separada, IDs efetivamente congelados, releitura da fonte normativa `C:\Users\Wandrey\OneDrive\Downloads\NFeAbi`, hashes conferidos contra o catálogo, build-base verde e ambiente correspondente disponível.
