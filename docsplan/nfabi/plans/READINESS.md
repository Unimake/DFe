# Readiness

`ABI-001` foi aprovada explicitamente pelo DEV em 2026-09-14 na `attempt-0003`. O plano ativo reside integralmente em `docsplan/nfabi`, está restrito à DLL Unimake.DFe e aos testes deste repositório; produtos consumidores externos possuem planejamento separado. Os IDs `ABI-002` a `ABI-006` estão congelados e o planejamento está `READY_FOR_EXECUTION`.

`ABI-002` está `DELIVERED_FOR_REVIEW` na `attempt-0001`: os 20 XSDs oficiais foram copiados byte a byte e embutidos, a fundação de enums, detecção e validação está coberta por testes, os hashes da fonte foram reconferidos no Check e a revisão crítica independente terminou em PASS. A etapa aguarda aprovação exclusiva do DEV.

`ABI-003` permanece `PLANNED` e não foi iniciada. Cada etapa posterior continua exigindo aprovação da predecessora e autorização separada.
