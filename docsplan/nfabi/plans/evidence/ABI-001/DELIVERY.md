# Unimake.DFe - NF-e ABI - Entrega ABI-001

A revisão decisória foi retrabalhada na `attempt-0003`: o pacote NF-e ABI foi movido integralmente de `docs/` para `docsplan/nfabi/`, preservando sua estrutura interna. README, AGENTS, instruções, linter, manifests, links, evidências e hashes passaram a usar a nova raiz canônica.

O escopo permanece exclusivo da DLL Unimake.DFe e dos testes deste repositório. O estado final é `DELIVERED_FOR_REVIEW`; somente o DEV pode aprovar. O congelamento dos IDs ainda não é efetivo e a `ABI-002` não foi iniciada.
