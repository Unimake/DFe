# Unimake.DFe - NF-e ABI - Entrega ABI-001

A revisão decisória foi retrabalhada na `attempt-0002`: o plano NF-e ABI ativo agora altera e valida exclusivamente a DLL Unimake.DFe e seus testes neste repositório. Referências, tarefas, ambientes, riscos, testes e handoffs de produtos consumidores externos foram retirados; a ABI-006 passou a ser o gate final de qualidade da DLL em `ENV-DLL`.

O estado final é `DELIVERED_FOR_REVIEW`. Somente o DEV pode aprovar. O congelamento dos IDs ainda não é efetivo, a `ABI-002` não foi iniciada e os snapshots anteriores foram preservados.
