# Unimake.DFe - NF-e ABI - Entrega ABI-002

A ABI-002 entrega exclusivamente a fundação do tipo fiscal na DLL Unimake.DFe: 20 XSDs oficiais íntegros e embutidos, `TipoDFe.NFeABI`, modelo 77, serviços e tipos XML, detecção pública das quatro raízes de entrada, configuração central de schema e validação restrita do namespace geral e do detalhe de evento NFeABI.

As fixtures sintéticas cobrem documento principal, status, consulta e cancelamento. Não foram implementados modelo OO completo, transporte, endpoints ou integração com produto consumidor externo.

O estado final é `DELIVERED_FOR_REVIEW`. Somente o DEV pode aprovar a ABI-002; a ABI-003 não foi iniciada.
